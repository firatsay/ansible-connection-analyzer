# Known Limitations

This agent has intentional limitations:

- Cannot verify network reachability
- Cannot validate credentials
- Cannot detect firewall rules or ACLs
- Cannot identify issues outside provided input

The agent provides probabilistic analysis, not guaranteed diagnoses.
Human validation is always required.
